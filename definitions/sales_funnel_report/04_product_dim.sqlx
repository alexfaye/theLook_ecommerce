config {
    type: "view",
    name: "product_dim",
    tags: ["sales_funnel_report"],
    description: "Products detail with distribution centers"
}

select p.id as product_id, p.name as product_name, p.category as product_category, p.brand as product_brand,
p.department, p.retail_price, dc.name, dc.latitude, dc.longitude
from ${ref("products")} p
left join ${ref("distribution_centers")} dc
on p.distribution_center_id=dc.id